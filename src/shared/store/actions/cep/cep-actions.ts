import { createAction } from '@ngrx/store';

export const cepTrueState = createAction('[Change cep State] Change state of cep component to true');

export const cepFalseState = createAction('[Change cep State] Change state of cep component to false');
